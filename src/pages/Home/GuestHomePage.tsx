import { useState } from "react";

const GuestHomePage = () => (
  <div className="min-h-screen flex items-center justify-center bg-black text-white text-2xl font-bold">
    Banddy 게스트 홈 (비로그인 상태)
  </div>
);

export default GuestHomePage;
